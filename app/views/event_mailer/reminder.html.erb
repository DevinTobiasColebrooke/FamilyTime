<h1>Hi <%= @user.name %>,</h1>

<p>Just a reminder that you have an event coming up tomorrow:</p>

<div style="margin: 20px 0; padding: 15px; background-color: #f3f4f6; border-radius: 8px;">
  <h2 style="margin-top: 0; color: #1f2937;"><%= @event.title %></h2>
  <p>
    <strong>When:</strong> <%= @event.start_time.strftime("%l:%M %p") %> - <%= @event.end_time.strftime("%l:%M %p") %>
  </p>
  
  <% if @event.description.present? %>
    <p><%= @event.description %></p>
  <% end %>

  <% if @event.type == 'Meal' && @event.meal_items.any? %>
    <p><strong>Menu:</strong> <%= @event.meal_items.pluck(:name).to_sentence %></p>
  <% end %>
</div>

<p>
  <%= link_to "View Details", event_url(@event) %>
</p>

<p>See you there!</p>
