Hi <%= @user.name %>,

Just a reminder that you have an event coming up tomorrow:

<%= @event.title %>
When: <%= @event.start_time.strftime("%l:%M %p") %> - <%= @event.end_time.strftime("%l:%M %p") %>

<% if @event.description.present? %>
<%= @event.description %>
<% end %>

<% if @event.type == 'Meal' && @event.meal_items.any? %>
Menu: <%= @event.meal_items.pluck(:name).to_sentence %>
<% end %>

View Details: <%= event_url(@event) %>
