<div class="container mx-auto px-4 py-8 max-w-2xl">
  <%= link_to "← Back to Board", events_path, class: "text-blue-600 hover:underline mb-4 inline-block" %>
  
  <div class="bg-white shadow rounded-lg p-6">
    <h1 class="text-3xl font-bold text-gray-800 mb-6">Edit <%= @event.type %></h1>
    <%= render "form", event: @event %>
  </div>
</div>
