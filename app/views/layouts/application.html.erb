<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Family Time" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <%# Enable PWA manifest for installable apps (make sure to enable in config/routes.rb too!) %>
    <%#= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%# Includes all stylesheet files in app/assets/stylesheets %>
    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="bg-gray-50">
    <nav class="bg-white shadow-sm">
      <div class="container mx-auto px-4 py-4 flex justify-between items-center">
        <div class="flex items-center space-x-6">
          <%= link_to "FamilyTime", root_path, class: "text-xl font-bold text-blue-600" %>
          <% if Current.user %>
            <%= link_to "Board", events_path, class: "text-gray-600 hover:text-gray-900 #{'font-bold text-gray-900' if current_page?(events_path) || current_page?(root_path)}" %>
            <%= link_to "Calendar", calendar_path, class: "text-gray-600 hover:text-gray-900 #{'font-bold text-gray-900' if current_page?(calendar_path)}" %>
            <%= link_to "Grocery List", grocery_list_path, class: "text-gray-600 hover:text-gray-900 #{'font-bold text-gray-900' if current_page?(grocery_list_path)}" %>
          <% end %>
        </div>
        
        <div class="flex items-center space-x-4">
          <% if Current.user %>
            <%= link_to "Hi, #{Current.user.name}", edit_users_preference_path, class: "text-gray-600 hover:text-gray-900 font-medium" %>
            <%= button_to "Sign out", sign_out_path, method: :delete, class: "text-gray-500 hover:text-gray-700 text-sm" %>
          <% else %>
            <%= link_to "Sign in", sign_in_path, class: "text-blue-600 hover:text-blue-800" %>
            <%= link_to "Sign up", sign_up_path, class: "bg-blue-600 hover:bg-blue-700 text-white px-4 py-2 rounded" %>
          <% end %>
        </div>
      </div>
    </nav>

    <% if notice %>
      <div class="bg-green-100 border-l-4 border-green-500 text-green-700 p-4 mb-4 container mx-auto mt-4" role="alert">
        <p><%= notice %></p>
      </div>
    <% end %>
    <% if alert %>
      <div class="bg-red-100 border-l-4 border-red-500 text-red-700 p-4 mb-4 container mx-auto mt-4" role="alert">
        <p><%= alert %></p>
      </div>
    <% end %>

    <main class="container mx-auto px-4 pb-12">
      <%= yield %>
    </main>
  </body>
</html>
